apiVersion: v1
kind: Service
metadata:
  name: todo-backend-service
  namespace: todo-ns
spec:
  type: NodePort
  selector:
    app: todo-backend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008